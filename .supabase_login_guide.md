# 🔐 Supabase 로그인 가이드

## 방법 1: 브라우저를 통한 자동 로그인 (권장)

터미널에서 다음 명령을 실행하고 **Enter 키를 눌러** 브라우저를 엽니다:

```bash
supabase login
```

그 다음:
1. 브라우저가 자동으로 열립니다
2. Supabase 계정으로 로그인
3. "Authorize" 버튼 클릭
4. 성공 메시지가 표시되면 터미널로 돌아옵니다

---

## 방법 2: 액세스 토큰 수동 설정

브라우저 방식이 작동하지 않으면 이 방법을 사용하세요:

### 1단계: 액세스 토큰 생성

1. 브라우저에서 https://supabase.com/dashboard/account/tokens 접속
2. "Generate new token" 클릭
3. 토큰 이름 입력 (예: "Local Dev")
4. 생성된 토큰 복사 (한 번만 표시됩니다!)

### 2단계: 환경 변수 설정

**macOS/Linux (현재 세션):**
```bash
export SUPABASE_ACCESS_TOKEN="sbp_여기에_복사한_토큰_붙여넣기"
```

**영구 설정 (권장):**
```bash
# ~/.zshrc 파일에 추가
echo 'export SUPABASE_ACCESS_TOKEN="sbp_여기에_복사한_토큰_붙여넣기"' >> ~/.zshrc

# 설정 즉시 적용
source ~/.zshrc
```

### 3단계: 토큰 확인
```bash
echo $SUPABASE_ACCESS_TOKEN
# 토큰이 출력되어야 함
```

### 4단계: 프로젝트 연결
```bash
cd "/Users/SonDongMin/Desktop/Local Food Review App"
supabase link --project-ref pqvtdkcapilwfrzduosn
```

---

## 방법 3: Docker를 사용한 로컬 개발 (선택사항)

서버 배포 없이 로컬에서만 개발하려면:

```bash
# Supabase 로컬 환경 시작
supabase start

# 로컬 URL 사용하도록 코드 수정
# API_BASE_URL = "http://localhost:54321/functions/v1/..."
```

---

## ✅ 성공 확인

프로젝트 연결 후 다음 명령으로 확인:

```bash
# 프로젝트 상태 확인
supabase status

# Edge Function 배포
supabase functions deploy make-server-98b21042
```

---

## 🆘 문제 해결

### "Access token not provided" 에러
→ 방법 2의 환경 변수 설정 사용

### "Invalid access token" 에러  
→ 토큰 재생성 필요 (유효기간 만료 가능)

### "Project not found" 에러
→ 프로젝트 ID 확인: `pqvtdkcapilwfrzduosn`

### "Permission denied" 에러
→ Supabase 대시보드에서 프로젝트 접근 권한 확인

---

## 📞 추가 도움

- Supabase CLI 문서: https://supabase.com/docs/guides/cli
- Supabase Discord: https://discord.supabase.com/
